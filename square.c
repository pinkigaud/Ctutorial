// Write a program to calcuate the area of a square .(side is given)
#include<stdio.h>
int main(){
    float side;
    printf("Enter the side of the square: ");
    scanf("%f", &side);
    printf("The area of the square is: %f", side*side);
    return 0;
}
